# Challenge

TODO better readme

To run the challenge, start the db, frontend and backend with `docker-compose up --build db frontend backend`. The challenge bot does not run in Docker so far. Start the bot with `python3 main.py` in `./challenge/challenge-bot`. 